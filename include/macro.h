/*
** EPITECH PROJECT, 2024
** macro
** File description:
** header for macros
*/

#ifndef MACRO_H
    #define MACRO_H

    #define ABS(x) (((x) > 0) ? (x) : (-(x)))
    #define MAN(x, y) (((x) > (y)) ? (x) : (y))
    #define MIN(x, y) (((x) < (y)) ? (x) : (y))

#endif
